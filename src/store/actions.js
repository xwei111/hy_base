// import * as types from './mutationTypes'
// import { login } from 'api/login'

// export const setName = ({ commit, state }) => {
//   const p = new Promise((resolve, reject) => {
//     login().then(data => {
//       if (data.code === 200) {
//         commit(types.SET_NAME, data.result[0].name)
//         resolve(data.code)
//       }
//       reject('error')
//     })
//   })
//   return p
// }
